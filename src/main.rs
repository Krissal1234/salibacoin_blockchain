mod blockchain;
mod block;
mod errors;

fn main() {
let mut salibacoin_chain = blockchain::Blockchain::new();
let transaction = String::from("transaction");
// salibacoin_chain.add_block(transaction);
}
